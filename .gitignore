# 開発用ページ (seed-demo は開くと localStorage を上書きするため絶対に公開しない)
seed-demo.html
share-preview.html
.claude/
# 内部戦略ドキュメント (Pagesで公開配信されるため絶対にpush/デプロイしない)
docs/
# Capacitor / iOS ビルド (node_modules と www/ は生成物。ios/ はMac側で cap add ios 後に別途コミット)
node_modules/
www/
